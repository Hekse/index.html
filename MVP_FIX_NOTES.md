# Ajo MVP fix branch

This branch is for final MVP corrections before touching main.

Protected original branch: `ajo-mvp-original`
Working branch: `ajo-mvp-branch`

Planned corrections:
- Replace old Ajo splash with the same Opero splash style used in Kontakti, product text AJO.
- Replace old CSS-drawn logo with the shared Opero SVG logo style.
- Fix Meno / Meno-paluu so it is stored as structured trip type and shown in the PDF.
- Add Tyyppi column to the PDF report.
- Keep the current main branch unchanged until the test build is approved.
